# publish ---------------------------------------------------------------------
# Étape 5 : publication. Upsert du payload vers la cible (Supabase plus tard ;
# JSON local maintenant). Interface `publish(payload, target)` stubbée au
# ticket 5 (issue #6).
