# run_pipeline ----------------------------------------------------------------
# L'entrée unique et documentée du pipeline (docs/architecture.md §Pipeline).
# Compose les cinq étapes : download -> filter -> compute -> vintage -> publish.
# Les étapes n'existent pas encore — elles arrivent avec les tickets 2 à 5
# (issues #3 à #6). Ce stub verrouille l'interface.

#' Exécuter le pipeline de bout en bout
#'
#' L'entrée unique et documentée du pipeline. Les étapes (téléchargement,
#' filtrage, calcul, vintages, publication) arrivent avec les tickets 2 à 5.
run_pipeline <- function() {
  stop(
    "run_pipeline() n'est pas encore implémenté — ",
    "voir les tickets 2 à 5 (issues #3 à #6)."
  )
}
